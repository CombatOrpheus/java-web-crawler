package com.webcrawler.backend.json;

/**
 * An enum that represents the status of a crawl process.
 */
public enum Status {
	/**
	 * The crawl process is currently active.
	 */
	ACTIVE,
	/**
	 * The crawl process is done.
	 */
	DONE;
}
